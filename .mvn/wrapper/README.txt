This project uses a minimal Maven wrapper scaffold.

To enable the official Maven Wrapper runtime, generate the wrapper files locally with Maven installed, or replace these files with the standard wrapper output.
